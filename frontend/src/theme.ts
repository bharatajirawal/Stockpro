import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: { main: '#3f51b5' },
    secondary: { main: '#e91e63' }
  },
  typography: {
    fontFamily: '"Roboto", "Segoe UI", "Calibri", "Trebuchet MS", sans-serif'
  }
});
